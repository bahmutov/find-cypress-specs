describe('user home tests', () => {
  // both tags are listed at the test level
  it('loads', { tags: ['@user', '@sanity'] }, () => {
    // this test should have effective tags ['@user', '@sanity']
  })
})
