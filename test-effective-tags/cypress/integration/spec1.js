describe('user tests', { tags: '@user' }, () => {
  it('loads', { tags: '@sanity' }, () => {
    // something here
    // this test should have effective tags ['@user', '@sanity']
  })

  it('works again', () => {
    // something here
  })
})
